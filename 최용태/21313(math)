#include <stdio.h>

int main(void)
{
	int N=0;
	int i;
	scanf("%d", &N);
	for (i = N; i > 1; i = i - 2) {
		printf("1 2");
		if (i - 2 != 0)
			printf(" ");
	}

	if (i == 1)
		printf("3");
	
	return 0;
}
